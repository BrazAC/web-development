<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS Tags</title>
  </head>

  <body>
    <h1>
      <%=title%>
    </h1>
    <p>Current second:
      Current second is <%= seconds %>
    </p>
    <%if (seconds % 2 === 0) {%>
    <ul>
      <%for(var i = 0; i < items.length; i++){%>
        <li><%=items[i]%></li>
      <%}%>
    </ul>
    <p><%- htmlContent %></p>
    <%- include("./footer.ejs") %>
    <%} else {%>
    <p>No items to display</p>
    <%- include("./footer.ejs") %>
    <%}%>
  </body>
</html>